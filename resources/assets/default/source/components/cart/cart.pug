.cart
  | @if ($cart_count == 0)
  .cart__empty Ваша корзина покупок пуста.
  | @else
  .cart__content
    include ./cart-ordering/cart-ordering
    | @if ($cart_step == 1)
    include ./cart-item/cart-item
  .cart__aside
    .cart-nav
      ul.cart-nav__list
        li.is-active Список покупок
        li Оформление заказа
        li Информация о заказе
      .cart-nav__panel
        .cart-nav__total
          p {{$cart_total}}
        .cart-nav__button
          a.button.button--small.button--red(href='') Начать оформление
    | @elseif ($cart_step == 2)
    +cartStep_2()
  .cart__aside
    .cart-nav
      ul.cart-nav__list
        li Список покупок
        li.is-active Оформление заказа
        li Информация о заказе
      .cart-nav__panel
        .cart-nav__total
          p {{$cart_total}}
        .cart-nav__button
          a.button.button--small.button--red(href='') Продолжить
        .cart-nav__link
          span Вернуться
    | @elseif ($cart_step == 3)
    +cartStep_3()
  .cart__aside
    .cart-nav
      ul.cart-nav__list
        li Список покупок
        li Оформление заказа
        li.is-active Информация о заказе
      .cart-nav__panel
        .cart-nav__total
          p {{$cart_total}}
        .cart-nav__button
          a.button.button--small.button--red(href='') Оформить
        .cart-nav__link
          span Вернуться
    | @endif


  | @endif
