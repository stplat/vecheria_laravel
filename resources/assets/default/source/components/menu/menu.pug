.menu__container
  nav.container
    ul.menu
      | @foreach ($menu as $menu_items)
      li
        span.menu__plug {{$menu_items['category']}}
        ul.menu__submenu
          | @foreach ($menu_items['subcategory'] as $slug => $subcategory)
          li
            a.menu__submenu-link(href="/{{$slug}}") {{$subcategory}}
          | @endforeach
      | @endforeach