document.addEventListener('DOMContentLoaded', () => {
  if (document.querySelector('.product-desc')) {
    const tabs = document.querySelectorAll('.product-desc__tab');
    const screens = document.querySelectorAll('.product-desc__screen')
  }
});